//! # Semantic Analysis
//! The semantic analyzer takes an AST and applies type checking and `auto` type
//! inference, among other things. It then finally produces an IR of the program


struct SemAnalyzer {}